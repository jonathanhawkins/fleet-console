/**
 * The simulator core, as a module folder: `sim/engine/index.ts` composes one
 * file per storyline and per command. This path is the stable import for
 * every host and test (`@/sim/engine`, `./engine`).
 */
export * from "./engine/index";
